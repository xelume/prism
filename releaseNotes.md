# Prism 0.6.0

- 新增可配置的状态栏额度显示，可选择关闭、5 小时、7 天或同时显示两种额度。
- 优化额度刷新机制：当前账号按状态栏显示需求刷新，后台账号降低频率；菜单刷新使用独立门槛，并在重置后主动确认额度。
- 请求失败时按账号进行 5、10、20、30 分钟退避，并继续遵循服务端 `Retry-After`。
- 简化账号操作，仅保留“添加账号…”和“删除账号”子菜单；切换账号时会自动保存离开账号的最新认证。
- 修复删除确认框中较长邮箱显示不完整的问题。
- 从登录认证识别团队、Business、Enterprise 和 Edu 工作区，并在邮箱后显示“团队”标记。
- 精简当前账号显示，移除重复的“当前”文字，保留勾选、粗体和不可点击状态。

已安装旧版本的用户可通过应用内更新安装本版，或下载 DMG 将应用拖入 Applications。

仍为临时签名、未经 Apple 公证的发行包；更新包签名用于校验来源，不能代替 Apple 签名或公证。
