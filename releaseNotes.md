# Prism 0.3.2

- 在退出桌面客户端前检查独立 Codex 进程；发现阻塞时先提示，避免客户端已经退出才报错。
- 识别 VS Code 标准扩展目录中的 Codex 后台进程，明确提示保存任务并完全退出 VS Code；仅关闭扩展面板可能无法停止后台进程。
- ChatGPT 退出后遗留的 Crashpad 崩溃上报进程不再被误判为认证阻塞进程。
- 保留安全保护：不会自动结束独立终端／IDE 进程，阻塞进程未停止时不会替换认证，退出后仍会复查。

已安装 0.3.0 或 0.3.1 的用户可通过应用内更新安装本版，或下载 DMG 将应用拖入 Applications。

仍为临时签名、未经 Apple 公证的发行包；更新包签名用于校验来源，不能代替 Apple 签名或公证。
