# Prism 0.6.1

- 精简账号额度显示，使用 `5h`、`7d` 和 `1mo` 标签、紧凑的剩余比例与重置时间。
- 按接口实际返回的时间窗口动态显示额度，不再为不存在的额度保留占位行。
- 状态栏额度选项简化为“关闭”“简略”和“全部”；“简略”自动显示当前账号最短的额度窗口。
- 支持识别 28 至 31 天的月额度，并将其重置时间纳入刷新调度。
- 将工作区账号标识调整为小号灰色 `team` 文本。
- 将主菜单最小宽度从 340 pt 收紧到 280 pt，同时保留长内容自动扩宽。
- 默认开启每天自动检查更新；仍不会自动下载或安装，用户可以随时关闭。

已安装旧版本的用户可通过应用内更新安装本版，或下载 DMG 将应用拖入 Applications。

仍为临时签名、未经 Apple 公证的发行包；更新包签名用于校验来源，不能代替 Apple 签名或公证。
