# Prism 0.3.7

- 重新设计 Prism App 图标，将 Xelume（星烁）的八向星形与蓝紫棱镜切面融合为统一的产品标识。
- 新增独立的 macOS 菜单栏图标，在 18px 与 36px 下使用更清晰、饱满的单色轮廓，并自动适配系统深浅外观。
- 拆分 App 与菜单栏图标的 SVG 源文件和生成流程，确保各自按实际显示尺寸进行光学优化。
- 将两份图标源文件纳入 Xcode 构建输入依赖，图标调整后可可靠触发 `.icns` 与菜单栏 PNG 重新生成。

已安装 0.3.6 或更早版本的用户可通过应用内更新安装本版，或下载 DMG 将应用拖入 Applications。

仍为临时签名、未经 Apple 公证的发行包；更新包签名用于校验来源，不能代替 Apple 签名或公证。
