# Prism 0.7.6

- 缩短临时网络或服务失败后的等待时间，按 15、30、60 秒重试，后续等待上限为 60 秒。
- 展开菜单时自动提前重试失败账号，每次失败后冷却 15 秒，避免重复请求；无需手动刷新按钮。
- 保留系统唤醒和网络恢复检查，并继续遵守服务端限流及认证失败的等待规则。

已安装旧版本的用户可通过应用内更新安装本版，或下载 DMG 将应用拖入 Applications。

仍为临时签名、未经 Apple 公证的发行包；更新包签名用于校验来源，不能代替 Apple 签名或公证。
