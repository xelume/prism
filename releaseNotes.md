# Prism 0.3.6

- 修复旧 `Codex Auth` 钥匙串条目导致文件认证用户被误报为“不支持的认证方式”的问题。
- 与已检查的官方 ChatGPT `26.825.51511` 保持一致，只读写默认 `~/.codex/auth.json`；历史钥匙串条目不会被读取、修改或删除。
- 移除 0.3.4–0.3.5 中不再适用的 Direct Keyring 探测、选择、授权和写入兼容代码。
- 明确拒绝显式配置的 `keyring`、`auto`、`ephemeral`、Secrets 或自定义登录策略，避免修改与官方客户端文件认证不一致的状态。

已安装 0.3.5 或更早版本的用户可通过应用内更新安装本版，或下载 DMG 将应用拖入 Applications。

仍为临时签名、未经 Apple 公证的发行包；更新包签名用于校验来源，不能代替 Apple 签名或公证。
