# Prism 0.3.1

- 展开菜单时显示已有额度，并在查询结果返回后实时更新，无需关闭菜单再打开。
- 增加 30 秒查询冷却，重复展开不再频繁请求；保留后台定时查询、限流退避和账号切换期间暂停。
- 精简菜单，移除独立刷新入口及定时刷新提示，保留账号额度和异常状态。
- 仅发现新版本时显示“更新至…”入口；仍可在关于窗口手动检查更新。

已安装 0.3.0 的用户可通过应用内更新安装本版，或下载 DMG 将应用拖入 Applications。

仍为临时签名、未经 Apple 公证的发行包；更新包签名用于校验来源，不能代替 Apple 签名或公证。
